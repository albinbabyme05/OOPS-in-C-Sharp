# Student Info Manager
# Focus: Encapsulation
# Hint:

- Create a Student class with private fields like name, age, marks.

- Use public getters and setters to access and update values.

- Add validation (e.g., age must be > 0, marks between 0–100).

﻿
